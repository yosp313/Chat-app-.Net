
Todo:
  [] Add so that each user can choose what is there preferred language @high 
  [] Add the translating api @high 
  [] Add the the feature that the messages get translated automatically @high 
  [] Add nice styling @low 
  [X] Update the docker image @critical
  [] Add CI/CD (Jenkins or Github actions) @low 
  [] Add database integration to save the chat history @high

Optional:
  [] Transform it from MVC to client/server with react as a frontend
  [] mobile support?
